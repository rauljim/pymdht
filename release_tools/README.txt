These tools are meant to be used prior release.

* check_bootstrap.py
Pings each one of the nodes in bootstrap.main and prints warnings if any of
them is down.
Pings each one of the nodes in bootstrap.backup and outputs a file containing
only the nodes which replied.
